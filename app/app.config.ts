export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      neutral: 'slate'
    },
    container: {
      base: 'w-full max-w-[80vw] mx-auto px-0 sm:px-6 lg:px-8'
    }
  }
})
